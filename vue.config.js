module.exports = {
    lintOnSave: false,
    css: {
        loaderOptions: {
          sass: {
          },
          scss: {
          }
        }, // Enable CSS modules for all css / pre-processor files. // This option does not affect *.vue files.
      },
}